
#include "molecule-class-info.h"

// We can think about a more efficient interface when this one works
//
std::vector<generic_vertex>
molecule_class_info_t::make_generic_vertices_for_atoms() const {
   std::vector<generic_vertex> v;
   return v;
}
